{
  "name": "playwright-with-js",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "npx playwright test",
    "report:clean": "rm -rf allure-results allure-report",
    "report:generate": "npx allure generate ./allure-results -o ./allure-report",
    "report:open": "npx allure open ./allure-report",
    "test:fresh": "npm run report:clean && npm run test && npm run report:generate && npx allure open ./allure-report"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.56.1",
    "@types/node": "^24.3.0",
    "allure-playwright": "^3.4.2"
  },
  "dependencies": {
    "exceljs": "^4.4.0",
    "playwright": "^1.56.1"
  }
}
